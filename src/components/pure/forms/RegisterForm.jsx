import React, { useState } from "react";

const initialData = [{ name: "", lastname: "", email: "", status: "" }];
const RegisterForm = () => {
  const [data, setData] = useState(initialData);

  return <div>LoginForm</div>;
};

export default RegisterForm;
